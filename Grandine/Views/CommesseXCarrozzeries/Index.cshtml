@model IEnumerable<Grandine.Models.CommesseXCarrozzerie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Carrozzeria.RagioneSociale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Commesse.Codice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Carrozzeria.RagioneSociale)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Commesse.Codice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }, new { @class = "btn btn-success" }) |
            @*@Html.ActionLink("Details", "Details", new { id = item.ID },new { @class = "btn btn-info" }) |*@
            @Html.ActionLink("Cancella", "Delete", new { id = item.ID }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
